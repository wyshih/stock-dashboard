# 公開展示站的相依套件。刻意**很少** —— 這個站只讀 public_data/ 裡預先算好的
# 檔案，不做任何推論、不算技術型態。
#
# 明確不需要（也不要加回來）：
#   TA-Lib        148 條說法的判定在 engine 端離線算好了（pattern_hits.parquet），
#                 而且它需要 C 二進位，Streamlit Community Cloud 裝不起來
#   scikit-learn  不載入任何模型、不做推論
#   lightgbm      同上
#   pyarrow 以外的資料庫 / 抓取套件：這個站不連任何外部資料源

streamlit==1.39.0
pandas==2.2.3
numpy==1.26.4
pyarrow==18.1.0
plotly==5.24.1
